# ==========================================================
# Patient-Service: dev profile configuration
# Imports local profile configuration (H2 DB, JPA settings)
# ==========================================================
spring.config.import=classpath:application-local.properties

# ==========================================================
# SpringDoc OpenAPI / Swagger UI (Disabled for dev profile)
# ==========================================================
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false

